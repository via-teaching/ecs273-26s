# Homework 4 — Client

React + Vite frontend for the HW4 full-stack stock visualization app.

## Setup

```bash
npm install
npm run dev
```

The app runs at `http://localhost:5173`. Requires the FastAPI backend running on `http://localhost:8000` (see the top-level README for full setup instructions).
