Runtime:
- When `npm run dev` is started, it'll call "PrepNewsArticles.py" which packages everything up in a .json file for the server to read. This is adding to the "predev" section of vite

Packages:
- Using 'papaparse' for csv parsing in javascript
- Python packages: 'json' and 'os'

AI Usage:
- Used to learn the basics of how to set up a react project since I have zero experience with front-end web development
- Used to format/learn syntax of Javascript, formatting intended logic to the appropriate layout/syntax
- Used for learning options of d3 package
- Used for help with incorporating callback functions for the line chart rendering
- Heavy use for figuring out how plots using d3 work, and how to make them look nice (also used alongside a tutorial to implement zooming functionality)
- In App.jsx my plots were not appearing so used this to figure out what was going wrong to fix it
- Also used for making the viewports of the webapp look nicer (appropriately sized margins, etc.)
- Used to figure out what to do for preparing news articles as the web-side of things can't directly parse folders without knowing their contents beforehand.
- Also used alongside tutorials for the react 'useState' implementation alongside bug fixes
- Bug fixes for special characters in extracted articles







